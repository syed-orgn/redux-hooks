name: Notify Abbas on PR

on:
  pull_request:
    types:
      - opened

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Abbas
        run: |
          echo "A new pull request has been opened!" \
          | mail -s "New Pull Request" syedabbasmuttaqizaidi@cedcommerce.com
