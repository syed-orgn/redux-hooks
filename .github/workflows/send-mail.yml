name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: |
          CURRENT_USER=${GITHUB_ACTOR}
          echo "Current user is: $CURRENT_USER"
