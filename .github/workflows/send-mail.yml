name: Send Email on Push

on:
  push:
    branches:
      - master

jobs:
  send-email:
    runs-on: ubuntu-latest
    steps:
      - name: Send Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: syedabbasmuttaqizaidi@cedcommerce.com
          password: PP@password110
          subject: New code pushed to repository
          body: Code has been pushed to the repository.
          to: zaidi.yam5@gmail.com
          from: syedabbasmuttaqizaidi@cedcommerce.com
          secure: true
          ignore_cert: true
          convert_markdown: true
          priority: low
          # options: "--insecure" # This is necessary if using Gmail
